name: Render Keep-Alive
on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Wake Render
        run: curl -sS --max-time 30 -o /dev/null https://yaran.onrender.com/ || true
